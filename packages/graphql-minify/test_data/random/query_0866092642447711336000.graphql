query RandomQuery($Query__enterprise__slug: String!) {
  enterprise(slug: $Query__enterprise__slug) {
    announcementCreatedAt
    avatarUrl
    databaseId
    descriptionHTML
    id
    slug
    url
    viewerIsAdmin
    websiteUrl
  }
}
